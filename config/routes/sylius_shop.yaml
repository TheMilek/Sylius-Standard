# Default locale (en_US) - no prefix
# This is the main shop route - route names remain as "sylius_shop_*"
sylius_shop:
    resource: "@SyliusShopBundle/Resources/config/routing.yml"
    defaults:
        _locale: en_US

# Dutch locale - with /nl prefix
# Route names will be "sylius_shop_nl_*" (prefixed to avoid conflicts)
sylius_shop_nl:
    resource: "@SyliusShopBundle/Resources/config/routing.yml"
    prefix: /nl_NL
    name_prefix: nl_
    defaults:
        _locale: nl_NL

# Polish locale - with /pl prefix
# Route names will be "sylius_shop_pl_*" (prefixed to avoid conflicts)
sylius_shop_pl:
    resource: "@SyliusShopBundle/Resources/config/routing.yml"
    prefix: /pl_PL
    name_prefix: pl_
    defaults:
        _locale: pl_PL

sylius_shop_payum:
    resource: "@SyliusPayumBundle/Resources/config/routing/integrations/sylius_shop.yaml"

sylius_payment_notify:
    resource: "@SyliusPaymentBundle/Resources/config/routing/integrations/sylius.yaml"

# see https://web.dev/change-password-url/
sylius_shop_request_password_reset_token_redirect:
    path: /.well-known/change-password
    methods: [GET]
    controller: Symfony\Bundle\FrameworkBundle\Controller\RedirectController::redirectAction
    defaults:
        route: sylius_shop_request_password_reset_token
        permanent: false
